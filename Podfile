# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'Dream Decore' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'lottie-ios'

  # Pods for Dream Decore
  pod 'Firebase/Core'                 # Basic Firebase setup (usually includes analytics)
  pod 'Firebase/Auth'                 # For authentication features
  pod 'Firebase/Firestore'            # For Firestore database
  pod 'Firebase/Storage'              # For Firebase Storage (optional, add if needed)

end
